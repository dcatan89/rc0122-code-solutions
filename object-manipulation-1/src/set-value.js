/* exported setValue */
// define a function with three parameters
// for the object use key property of the object and assign the value to it./
// return the object as the output
function setValue(object, key, value) {
  object[key] = value;
  return object;
}
